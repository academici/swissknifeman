{
  "_comment": "Source: anonymized production Laravel project. Скрипты двух независимых сборок: основная (дефолтный vite.config.js) и админская (--config vite.admin.config.js). CI выполняет build И build:admin.",
  "scripts": {
    "dev": "vite",
    "dev:admin": "vite --config vite.admin.config.js",
    "build": "vite build",
    "build:admin": "vite build --config vite.admin.config.js"
  }
}
