---
name: task-brief-template
bucket: general
version: 0.3.0
description: "Шаблон постановки сложных задач для агента: цель, границы, инварианты, явные предположения, out of scope, критерии приемки, definition of done. Плюс паттерн Plan→Clear→Execute: план и выполнение — в раздельных сессиях."
risk: draft
persona: oss-dev
tags: [workflow, template]
requires: []
produces_for: []
outputs: []
snippets: []
adapters: [claude, cursor, fable]
sha256: ""
---

# Task Brief Template

## Когда активировать

Используйте этот skill, когда задача неоднозначна или затрагивает несколько подсистем.

## Шаблон постановки

```text
Контекст:
- Коротко: где проблема/инициатива и почему это важно.

Цель:
- Какой результат должен получить пользователь/бизнес.

Границы:
- Что входит в задачу.

Out of Scope (не трогаем без явного запроса):
- Файлы/модули вне задачи, даже если «можно улучшить».
- Новые зависимости (composer/npm) — только по явному запросу.
- Рефакторинг соседнего кода.

Явные предположения:
- Агент ОБЯЗАН верифицировать каждый пункт до старта;
  при несоответствии — стоп, уточняющий вопрос.
- [ ] Предположение 1: ...
- [ ] Предположение 2: ...

Инварианты:
- Какие правила и контракты нельзя нарушать.

Затронутые слои:
- backend / frontend / policies / routes / docs / tests.

Ожидаемое поведение:
- До изменения:
- После изменения:

Критерии приемки:
- [ ] ...
- [ ] ...

Минимальная верификация:
- Какие тесты/проверки обязательно запустить.

Definition of Done:
- [ ] Все критерии приемки закрыты.
- [ ] Тесты и линт зелёные.
- [ ] git diff не содержит изменений вне Scope.
- [ ] Документация/CHANGELOG обновлены (если задача меняет публичное поведение).

Ограничения:
- Срок, риски, legacy-ограничения, обратная совместимость.
```

## Мини-чеклист качества постановки

- Цель описывает результат, а не только действия.
- Есть явные не-цели (out of scope) и запрет на правки вне scope.
- Предположения выписаны явно, а не сделаны молча.
- Перечислены инварианты домена и безопасности.
- Указан минимальный test-gate.
- Definition of Done отличим от критериев приемки: процессные условия
  (тесты, линт, диф в границах scope), а не поведение фичи.
- Зафиксирован формат финального отчета по задаче.

Заполнять шаблон по нечёткой идее помогает скилл `spec-interview`
(bucket general): интервью с вопросами по одному до старта кода.

## Plan → Clear → Execute

Для крупных задач бриф и выполнение разносятся по сессиям:

1. **Сессия плана**: обсудить задачу по шаблону выше, сохранить готовый бриф/план
   в файл (`.claude/plans/<task>.md`). План — самодостаточный: исполнитель не увидит
   текущее обсуждение.
2. `/clear` — промежуточные попытки планирования не тянутся в контекст выполнения.
3. **Сессия выполнения**: загрузить только файл плана и выполнять по шагам.

Готовые шаблоны команд `/plan` и `/execute` и остальная дисциплина экономии
контекста — в скилле `context-economy` (bucket general).
